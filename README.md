# procon-new

## sample, work, test の build

```
mkdir build
cd build
cmake ../
make
```

## コンテスト

- 上記のbuildで `main.cpp` がbuildされるため、活用するとよい
- workディレクトリで作業
- `bundle.sh` のようにするとライブラリが展開され、`main.cpp` の内容がクリップボードへコピーされる
